import { Flex } from "antd";

import styled from "styled-components";

export const FormContentWrapper = styled(Flex)({
  padding: "20px 0",
  display: "flex",
  flexDirection: "column",
  gap: "20px",
});
